export namespace IamEnums {
  export enum GRANT_TYPES {
    CLIENT_CREDENTIALS = 'client-credentials',
    PASSWORD = 'password',
    REFRESH_TOKEN = 'refresh-token',
  }

  export enum SESSION_TOKEN_TYPE {
    CLIENT_TOKEN = 'client-token',
    USER_TOKEN = 'user-token',
  }
}
